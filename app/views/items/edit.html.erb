<div class="d-flex justify-content-between align-items-center mb-3">
  <div>
    <h4 class="fw-bold mb-0">Edit Item</h4>
    <div class="text-muted small">Update item information.</div>
  </div>
</div>

<%= render "form", item: @item %>